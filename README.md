# Snake Game in Java

Run from PowerShell:

```powershell
javac SnakeGame.java
java SnakeGame
```

Or double-click `run.bat`.

Controls:

- Arrow keys or WASD: move
- Space: pause/resume
- Enter: restart
